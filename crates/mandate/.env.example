# Only needed when the gateway runs with MANDATE_MODE=ens (see
# crates/gateway/.env.example) — resolves mandate policy from the live
# ENSv2 PermissionedRegistry instead of the in-memory mock.

SEPOLIA_RPC_URL=https://sepolia.example.com
MANDATE_REGISTRY_ADDRESS=0x0000000000000000000000000000000000000000
